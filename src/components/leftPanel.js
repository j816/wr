class LeftPanel {
  constructor() {
    this.initTabs();
    this.initWritingPromptsTab();
    this.initAIAssistantTab();
  }

  initTabs() {
    // Tab initialization code specific to Left Panel
  }

  initWritingPromptsTab() {
    // Initialize Writing Prompts Tab without interfering with other panels
  }

  initAIAssistantTab() {
    // Initialize AI Assistant Tab independently
  }
}

// Initialize Left Panel
document.addEventListener('DOMContentLoaded', () => {
  new LeftPanel();
}); 